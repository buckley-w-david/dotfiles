- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc.common:
    ~/.editorconfig:
    ~/.gitconfig:
    ~/.gvimrc:
    ~/.inputrc:
    ~/.muttrc:
    ~/.tmux.conf:
    ~/.tridactylrc:
    ~/.vimrc:
    ~/.xmonad/xmonad.hs: 
        create: true
        path: xmonad/xmonad.hs
    ~/.xmobarrc:
    ~/.mutt:
        glob: true
        create: true
        path: mutt/*
    ~/scripts: 
      glob: true
      create: true
      path: scripts/*
    ~/.tmuxp:
        glob: true
        create: true
        path: tmuxp/*

- shell:
  - [git submodule update --init --recursive, Installing submodules]
