[user]
	email = buckley.w.david@gmail.com
	name = David Buckley
[alias]
	logtree = log --graph --oneline --decorate --all
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
